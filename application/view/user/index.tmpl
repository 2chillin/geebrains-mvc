<html>
<head></head>
<body>
<h2>{{title}}</h2>
{% include 'menu.tmpl' %}
{% if user %}
    <p>Добро пожаловать, {{ user }}!</p>
    <p>{{ message }}</p>
    <ul>
        <li><a href="./?path=orders/index">Заказы</a></li>
    </ul>
    <form method="post" action="?path=auth/logout">
        <button type="submit">Выйти</button>
    </form>
{% endif %}

{% if user==false %}
    <p>Для использования личного кабинета необходима авторизация!</p>
    <p>{{ message }}</p>
    <form method="post" action="./?path=auth/login">
        <label for='login'>Логин: </label><input type="text" name="login"><br><br>
        <label for='pass'>Пароль: </label><input type="text" name="pass"><br><br>
        <button type="submit">Войти</button>
    </form>
{% endif %}

</body>
</html>
