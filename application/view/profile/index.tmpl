<html>
<head></head>
<body>
<h2>{{title}}</h2>

{% if auth %}
    <p>Добро пожаловать, {{ auth }}!</p>
    <p>{{ message }}</p>
    <form method="post" action="?path=profile/logout">
        <button type="submit">Выйти</button>
    </form>
{% endif %}

{% if auth==false %}
    <p>Для использования личного кабинета необходима авторизация!</p>
    <p>{{ message }}</p>
    <form method="post" action="?path=profile/login">
        <label for='login'>Логин: </label><input type="text" name="login"><br><br>
        <label for='pass'>Пароль: </label><input type="text" name="pass"><br><br>
        <button type="submit">Войти</button>
    </form>
{% endif %}

</body>
</html>
